<div class="container">

  <h1 class="mt-4"><%= @product.title %></h1>
  <div>
    <%= Category.find(@product.category_id).title %>
  </div>
  <hr>

  <p>
    <%= @product.description %>
  </p>
  <!-- <p>
    <%# <%= image_tag @product.image.url(:thumb), class: "img-show" if  @product.image? %> %>
  </p> -->

  <% @product.images.each do |images| %>
     <%= image_tag images.url(:large) %>
  <% end %>

    <%= link_to 'Back', admin_products_path, class:"btn btn-outline-primary"%>
</div>
